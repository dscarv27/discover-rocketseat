function showMessages(message: string): string{ //a tipagem após o parâmetro da função indica o tipo de retorno
    return message
}

function dontShowMessage(otherMessage : string): void{
//não há retorno pois a tipagem da função é void
}